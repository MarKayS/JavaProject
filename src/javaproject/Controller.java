package javaproject;

import java.awt.*;
import java.util.Scanner;

/**
 * Created by MarKay on 3. 3. 2015.
 *//*
public class Controller {
    public void assumeControl(){
        boolean gameover = false;
        Point player = locate();
        while(!gameover){
            System.out.print("Make your choice - up/down/left/right \n");
            Scanner scanner = new Scanner(System.in);
            String choice = scanner.nextLine();

        }
    }
    public Point locate(){
        for(int i=0;i<arrayY.size();i++){
            for(int ii=0;ii<arrayX.size();i++){
                if(arrayY.get(i).get(ii).equals("P")){
                    return new Point(ii,i);
                }
            }
        }
        return new Point(-1,-1); // locate not successful
    }
    public boolean move(char direction, Point player){
        Point destination;
        if(direction=='u'){
            destination = new Point(player.x,player.y+1);
        }
        else if(direction=='d'){
            destination = new Point(player.x,player.y-1);
        }
        else if(direction=='r'){
            destination = new Point(player.x+1,player.y);
        }
        else{
            destination = new Point(player.x-1,player.y);
        }

        if(level.isEmpty(destination)){

        }
        return false;
    }

}
*/
